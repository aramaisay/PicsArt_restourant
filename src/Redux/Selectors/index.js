// questionable at least
export const cartSelector = (state) => [state.cart, state.price];
export const restourantSelector = (state) => state.restourants;
export const dishesSelector = (state) => state.dishes;
export const kitchenTypeSelector = (state) => state.types;
